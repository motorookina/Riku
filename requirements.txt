# db-ops-v3 runtime dependencies (Windows / Linux)
#
# Simplified from a Windows `pip freeze`:
#   - removed Windows-only packages (e.g. pywin32)
#   - removed unused AI-provider integrations (langchain-openai/anthropic/google-genai, ...)
#   - direct deps only; transitive deps are resolved by pip
# Versions are pinned to match the development environment.

# AI framework
langchain==1.3.15
langchain-core==1.5.4
langchain-deepseek==1.1.0
langgraph==1.2.11
langgraph-checkpoint==4.2.0      # MemorySaver (short-term memory)
langgraph-checkpoint-postgres==3.1.0  # PostgresSaver (durable session checkpoint)
psycopg[binary]                  # Psycopg 3 driver (PostgresSaver 依赖)
deepagents==0.7.5

# HTTP client（openai SDK 的传递依赖；流式场景直接捕获 httpx 传输层异常，显式声明）
httpx

# Docker sandbox
docker==7.2.0

# General
python-dotenv==1.2.2
PyYAML==6.0.3
